// drag.utils.ts - stub for event drag/resize integration
export const initDragHandlers = (_container: HTMLElement | null) => {
  // To enable: install interactjs and wire up handlers.
  return () => { /* cleanup */ };
};
